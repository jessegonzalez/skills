Argo Rollouts "{{ .Values.strategy }}" Rollout deployed as {{ include "argo-rollouts-skill.fullname" . }}.

Watch it:
  kubectl argo rollouts get rollout {{ include "argo-rollouts-skill.fullname" . }} -w

{{- if eq .Values.strategy "bluegreen" }}
Blue-green with autoPromotionEnabled={{ .Values.bluegreen.autoPromotionEnabled }}.
{{- if not .Values.bluegreen.autoPromotionEnabled }}
Promote the preview when ready:
  kubectl argo rollouts promote {{ include "argo-rollouts-skill.fullname" . }}
{{- end }}
{{- else }}
Advance a paused canary:
  kubectl argo rollouts promote {{ include "argo-rollouts-skill.fullname" . }}
{{- end }}

Abort a bad rollout:
  kubectl argo rollouts abort {{ include "argo-rollouts-skill.fullname" . }}
